# Cool Fireworks

Render colorful ASCII fireworks directly in your terminal.

## Running

Use Python's module runner to start the demo:

```bash
python -m coolfireworks --duration 3 --density 0.1
```

Press `Ctrl+C` to exit early.

## Testing

```bash
pytest -q
```
